basePath: /
definitions:
  forms.CreatePostBody:
    properties:
      content:
        type: string
      imgUrl:
        type: string
      tags:
        description: JSON 数组
        items:
          type: string
        type: array
      title:
        type: string
    required:
    - content
    - imgUrl
    - tags
    - title
    type: object
  forms.PostResponse:
    properties:
      adjust_time:
        type: string
      id:
        type: integer
      img_url:
        type: string
      title:
        type: string
    type: object
  utils.ErrorResponse:
    properties:
      code:
        type: integer
      errorMessage:
        type: string
      message:
        type: string
    type: object
host: localhost:8080
info:
  contact:
    email: support@swagger.io
    name: xinghe
    url: http://www.swagger.io/support
  description: 基于 Gin 的博客后端 API
  termsOfService: http://swagger.io/terms/
  title: 我的博客 API
  version: "1.0"
paths:
  /blog:
    post:
      consumes:
      - application/json
      description: 根据传入的参数创建一篇新文章
      parameters:
      - description: 文章信息
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/forms.CreatePostBody'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/forms.PostResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/utils.ErrorResponse'
      summary: 创建文章
      tags:
      - blog
swagger: "2.0"
